import { ISensorDataGateway } from '../../interfaces/ISensorData';
import {  SensorData } from '../../models';

export const saveSensorData = async (sensorData: ISensorDataGateway[] = []) => {
	for (const item of sensorData) {
		}
	}

